# ----------------------------------------
# ✅ Static Site Routing + Caching
# ----------------------------------------

RewriteEngine On
RewriteBase /

# 1️⃣  Serve existing files/folders (Images, Icons, etc.)
# If the requested file actually exists on the server, serve it directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule . - [L]

# 2️⃣  Redirect everything else to index.html
# This prevents "404 Not Found" errors. If someone types a wrong link,
# they will just see your main "Coming Soon" page.
RewriteRule ^ index.html [L]

# ----------------------------------------
# 🧠  Caching Rules
# ----------------------------------------

# 🗂️  No-cache for the main HTML file
# This ensures that if you change the text or links, users see it immediately.
<FilesMatch "^index\.html$">
  Header set Cache-Control "no-store, no-cache, must-revalidate"
</FilesMatch>

# 💾  Cache Images for 1 Month
# Since your images don't have random hash names, we cache them for 1 month
# instead of 1 year, just in case you update a photo.
<FilesMatch "\.(jpg|jpeg|png|gif|svg|ico)$">
  Header set Cache-Control "public, max-age=2628000"
</FilesMatch>

# 🧾  Enable Gzip Compression (Speeds up loading)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css application/javascript application/json
</IfModule>